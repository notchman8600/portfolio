version: "3"
services:
  app:
    container_name: nuxt_sample
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    ports:
        - 3001:3000
    volumes:
        - .:/app
        - /app/node_modules
